extends ./layout

block content
  .container
    .clearfix
      form.form-signin(action='/forget-password', method='POST')
        label(for='email') Enter Email
        input(
          type='email', id='email', name='email', class='form-control', 
          placeholder='Email address', required, autofocus
        )