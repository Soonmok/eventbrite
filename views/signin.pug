extends ./layout

block content
  .container
    .clearfix
      form.form-signin(action='/signin', method='POST')
        h3.form-signin-heading Lets get started
        label.sr-only(for='email') Enter Email
        input(
          type='email', id='email', name='email', class='form-control', 
          placeholder='Email address', required, autofocus
        )

        label.sr-only(for='password') Enter Email
        input(
          type='password', id='password', name='password', class='form-control', 
          placeholder='password', required, autofocus
        )
        
        button.btn.btn-primary.btn-block(type='submit') Get Started
        a.btn.btn-dark.btn-block(href='/auth/facebook')
          i.fa.fa-facebook.fa-lg
          | &nbsp;
          | Signin with Facebook
        
        a.btn.btn-dark.btn-block.fa-kakaotalk(href='/auth/kakao')
          i.fa.fa-lg
          | &nbsp;
          | 


        div.sub 
          a(href='/users/new') Signup
          | 
          | /
          |
          a(href='/forget-password') Reset Password